# Opt-in extra patterns (action input: strict: true).
#
# Bare vendor keywords and `.claude/` path references. Deliberately NOT
# default: on devcontainer-template's own history `.claude/` appears in 25 of
# the last 60 legitimate commit messages ("fix: update .claude/scripts/…").
# Enable on repos where a commit message has no legitimate reason to mention
# the tooling.
claude[-[:space:]]code
\bclaude\b
\banthropic\b
\.claude/
(see|ref|tracked-in)[[:space:]]+\.claude
