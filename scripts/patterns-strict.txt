# Opt-in extra patterns (workflow input: strict: true).
#
# Two classes that are deliberately NOT default:
#   1. Bare vendor keywords — high false-positive rate on repos whose
#      subject matter is the tooling itself.
#   2. Workflow-internal leaks — paths and footers that reveal an
#      agent-driven workflow rather than an AI author (ktn-linter #364).
#      Repos that legitimately version a .claude/ directory would fail on
#      every commit that touches it.
claude[-[:space:]]code
\.claude/
^[[:space:]]*plan:[[:space:]]
(see|ref|tracked-in)[[:space:]]+\.claude
